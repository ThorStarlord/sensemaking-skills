interface:
  display_name: Project Sensemaker
  short_description: Turn vague project uncertainty into a clear problem frame, research path, and next-step prompt.
  icon: 🧭
  color: blue

instructions: >
  Follow the workflow and 12-section output format defined in SKILL.md. 
  Always produce a "Sensemaking Brief" as your primary output. 
  Consult the skill-registry.yaml to recommend the best downstream skill or ecosystem. 
  Adhere strictly to the Boundary Rule: do not perform downstream work unless explicitly requested.

tools:
  - web_search
  - read_local_files
  - search_local_files
