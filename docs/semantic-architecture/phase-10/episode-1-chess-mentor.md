# Phase 10 Episode 1 — Chess Mentor Engine / direct diagnosis

**Source capability:** `repo-sensemaker` reasoning discipline  
**Target:** `ThorStarlord/Chess-Mentor-Engine@4139e4dda567feeadb5684f51c4a265dfae07330`  
**Access:** read-only GitHub exact-SHA evidence  
**Companion profile:** `episode-1-chess-mentor-profile.yaml`

## Episode result

The pinned repository already has a strong local evidence/claim-ceiling discipline. M19 is explicitly qualified as provenance-bound model language rather than semantic model truth, and the handoff names model-output evaluation as the next priority before production-provider adoption.

The common profile therefore did **not** discover a hidden architectural defect. Its useful contribution was different: it compressed the currentness boundary, observed facts, inferred next boundary, one decision-changing uncertainty, and explicit non-claims into a small cross-Skill representation.

## What the profile added

Material value:

- made the exact target SHA and access surface first-class rather than leaving them distributed across handoff prose;
- separated the observed M19 claim ceiling from the inferred conclusion that semantic/evaluator-facing qualification is the next weakest boundary;
- made the unresolved evaluator-authority split explicit: mechanical grounding checks versus semantic evaluation;
- preserved explicit non-claims in a form a fresh context can scan without treating the recommendation as authorization.

## What it duplicated

The repository's own `STATUS.md` and build-status document already contain unusually strong claim ceilings, qualification evidence, and next-step caveats. The profile repeats much of that semantic content.

This matters for Phase 10: the profile's value here comes from **cross-artifact compression and reconstruction**, not because the canonical repository handoff is semantically deficient.

## Embedding assessment

Embedding the whole profile into a repository sensemaking brief would likely add ceremony because repo-sensemaker already carries target/currentness, evidence, uncertainty, and limits in its native artifact contract.

No missing field in this episode warrants a canonical schema change.

**Episode signal:** favors **Outcome A — keep companion**, with moderate reconstruction value and moderate duplication.

## Measured/observed experiment notes

- Material omission caught by profile: **no hidden omission; one inference/observation boundary made more explicit**.
- Fresh-context utility: **high** for quickly recovering the M19 claim ceiling and unresolved evaluator boundary.
- Domain-semantic duplication: **moderate-high**.
- Validator overreach observed: **none**; the profile can represent the inference without validating its truth.
- Local-only probe metrics: **unmeasured on connector surface**.
- Exact token overhead: **not instrumented**; coordination overhead is qualitatively moderate and should not be converted into a fake precision metric.

## Non-claim

This episode does not prove that a model-output evaluation harness is the best product investment; it establishes only that the current repository evidence makes that boundary the strongest next engineering candidate visible on the inspected snapshot.
