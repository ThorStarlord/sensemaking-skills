# PM dogfood and empirical qualification status

**Protocol:** `pm-customer-discovery-v1`  
**Repository source-methodology migration:** COMPLETE through Waves 1–6  
**Repository implementation merge frontier:** `main@d433d7d6c34cc8a529b35b2bbfec2a1426c8c3f9` (PR #321 merge)  
**Checked-in non-qualifying preflights:** 1  
**Checked-in real-harness functional attempts:** 0  
**Checked-in second-harness portability attempts:** 0  
**Current functional empirical PASS:** NONE  
**Current portability empirical PASS:** NONE  
**External native-harness action required for stronger support/promotion claims:** YES

## What repository evidence now proves

All six PM implementation waves have reached `REPOSITORY_QUALIFIED` on merged exact candidate heads. The pinned 27-command upstream methodology inventory has been reconciled into current canonical PM capabilities, with upstream `prd` merged into the pre-existing `to-prd` authority rather than duplicated.

Repository evidence proves, at the mechanical/product-contract level, that:

- canonical PM Skill trees exist without requiring one coding-agent runtime for their semantic definition;
- relevant PM artifacts route through bounded specialized deterministic validators;
- valid artifacts can be admitted as exact Campaign evidence and invalid representations fail closed where covered by the contracts;
- PM capabilities are declared in the current Campaign capability catalog by explicit responsibility type without deterministic semantic ranking;
- exact-head Product Validation, retained Lab Validation where applicable, and Release Candidate Distribution have qualified the merged implementation waves;
- the existing adapter/setup system can represent canonical Skill trees for multiple supported harness roots.

These are repository/structural claims. They are not evidence that a real Claude Code, Codex, OpenCode, or other supported coding-agent harness natively discovered and invoked the PM Skills.

## Non-qualifying preflight evidence

A repository-side preflight was executed on 2026-09-10 using a qualified release-candidate distribution and repository evidence from `ThorStarlord/Chess-Mentor-Engine@4139e4dda567feeadb5684f51c4a265dfae07330`.

The preflight:

- initialized a durable Campaign and advanced into explicit `problem_discovery` responsibility;
- exposed the unranked canonical `discovery` capability candidate;
- produced a contract-valid `discovery_findings` artifact;
- admitted the exact bytes through the PM validator;
- closed with `evidence_insufficient` rather than turning repository roadmap evidence into customer truth;
- wrote a handoff, passed Campaign validation, reconstructed successfully through resume, and preserved transition/evidence lineage.

The environment had no Codex, Claude Code, or OpenCode executable and no configured OpenAI/Anthropic API credential. The artifact therefore was not produced through a supported harness's native Skill discovery/invocation path. This preflight remains deliberately **non-qualifying** and the real-harness attempt count remains zero.

Preserved evidence lives under:

`docs/product-management/dogfood/preflights/2026-09-10-chess-mentor-engine/`

## Qualification-debt policy

The program uses:

```text
CANDIDATE
-> REPOSITORY_QUALIFIED
-> NATIVE_HARNESS_QUALIFIED
-> PORTABILITY_QUALIFIED
-> PROMOTED
```

and:

```text
dogfood before promotion
!=
dogfood before expansion
```

The repository implementation phase is now complete for the pinned upstream source inventory. Native-harness and second-harness validation remain required for corresponding stronger claims and promotion.

No amount of repository-local CI, artifact validation, Campaign admission, adapter parity, or additional source-methodology migration should silently change the empirical counters above.

## Required future empirical evidence

Follow `docs/product-management/dogfood-runbook.md`:

1. run a real PM responsibility/Campaign on one supported native harness;
2. preserve the harness's actual Skill discovery/invocation evidence;
3. perform a fresh-context reconstruction from durable repository/Campaign state without prior chat;
4. run an equivalent bounded responsibility through a second supported native harness;
5. preserve exact attempt evidence and classify each result honestly;
6. repair observed product defects before promotion where required.

Until that evidence exists, do not claim `NATIVE_HARNESS_QUALIFIED`, `PORTABILITY_QUALIFIED`, `PROMOTED`, or `FUNCTIONAL_PASS_PORTABILITY_PASS` for the PM domain.

## Current frontier

For PM source migration, there is no next Wave 7 implied by this program. The next PM frontier is **empirical qualification and evidence-derived product improvement**.

New PM capability work may still be separately authorized for new product requirements or methodologies, but it is not continuation of the completed pinned 27-command migration.
