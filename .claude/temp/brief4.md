> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Standardize validate-usage-research-report with consistent CLI, stable error codes, and machine-parseable output

**Current behavior:**
validate-usage-research-report.py has no `--repo-root` flag, no `--list-codes`, no `main(argv)` entry point, uses free-text error messages, and names its positional argument `report_path`.

**Desired behavior:**
The validator conforms to the standard contract: accepts `artifact_path` (positional), `--repo-root`, and `--list-codes`. Has a callable `main(argv)` entry point. Emits named error code constants and outputs `ERROR {CODE}: {message}`. Negative fixtures assert against stable codes.

**Key interfaces:**
- Standard CLI: `python validate-usage-research-report.py <artifact_path> [--repo-root PATH] [--list-codes]`
- `main(argv list | None) -> int` entry point
- Module-level error code constants: REPORT_FILE_NOT_FOUND, MISSING_SECTION, INVALID_SEMANTIC_SCORE, INVALID_FAILURE_CLASSIFICATION, PLACEHOLDER_DETECTED, ABSOLUTE_PATH_DETECTED, ROLE_BOUNDARY_VIOLATION
- Error output format: `ERROR {CODE}: {message}`

**Acceptance criteria:**
- [ ] `--repo-root` flag accepted (defaults to `.`)
- [ ] `--list-codes` prints all error codes and exits 0
- [ ] `main(argv)` callable programmatically
- [ ] Error output uses `ERROR {CODE}: {message}` format
- [ ] Negative fixture frontmatter uses stable codes, not free-text substrings
- [ ] `python scripts/test-validators.py` passes

**Out of scope:**
- No behavioral changes to validation logic
- No changes to validate-plan or other validators
