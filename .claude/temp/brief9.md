> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Remove hardcoded validator-name check from test harness

**Current behavior:**
The test harness has a hardcoded check that only passes `--repo-root` to specific validators by filename. This was needed because one validator lacked `--repo-root`.

**Desired behavior:**
All validators uniformly handle `--repo-root`. The test harness passes it to every validator without special-casing.

**Key interfaces:**
- The test runner function — remove filename-based condition, always pass `--repo-root`
- All Level 2 and Level 3 validators accept `--repo-root` (guaranteed by prerequisite slices)

**Acceptance criteria:**
- [ ] Hardcoded filename check removed from the test harness
- [ ] `--repo-root` passed uniformly to all validator invocations
- [ ] `python scripts/test-validators.py` passes

**Out of scope:**
- No changes to any validator script
- No changes to the fixture directory structure
- No changes to the report generation logic
