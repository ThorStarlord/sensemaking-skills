> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Create new specialized Level 3 validator for prompt_handoff artifacts

**Current behavior:**
The `prompt_handoff` artifact type (terminal artifact in 10 of 11 workflows, consumed by `external_agent` without human review) passes only through the generic Level 2 validator. No specialized checks exist for registry cross-references, stop condition quality, or hallucinated artifact IDs.

**Desired behavior:**
A new validator checks prompt_handoff artifacts for structural completeness, registry validity, and content quality. Uses the standard CLI contract and shared utility module. Registered in artifact-contracts.yaml and covered by fixtures.

**Key interfaces:**
- Standard CLI: `python validate-prompt-handoff.py <artifact_path> [--repo-root PATH] [--list-codes]`
- Uses shared module for loading skill-registry.yaml and artifact-contracts.yaml
- Error code constants: MISSING_SECTION, UNKNOWN_TARGET_SKILL, EMPTY_STOP_CONDITION, EMPTY_EXPECTED_OUTPUT, HALLUCINATED_ARTIFACT_REF, ABSOLUTE_PATH_DETECTED, MISSING_READY_PROMPT
- Registered in artifact-contracts.yaml as `specialized_validators` for the `prompt_handoff` artifact

**Acceptance criteria:**
- [ ] Validator script exists with standard CLI
- [ ] Uses shared utility module for registry loading
- [ ] All 7 error codes have corresponding checks
- [ ] Validator registered in artifact-contracts.yaml
- [ ] Fixture directory exists with 1 valid + 5 invalid fixtures (one per unique error code)
- [ ] Regression entry added to REGRESSIONS.yaml
- [ ] `python scripts/test-validators.py` passes
- [ ] `python scripts/validate-repo.py` passes

**Out of scope:**
- No changes to the generic Level 2 validator (validate-artifact.py)
- No changes to any existing validators
