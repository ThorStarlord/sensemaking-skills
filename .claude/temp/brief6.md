> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Standardize validate-artifact with shared utilities, stable error codes, and consistent CLI

**Current behavior:**
validate-artifact.py loads artifact-contracts.yaml inline (duplicated from other validators), has no `--list-codes`, no `main(argv)` entry point, and uses free-text error messages. It already has the correct `artifact_id` + `artifact_path` two-positional signature.

**Desired behavior:**
The validator uses the shared utility module for registry loading. It has `--list-codes`, a `main(argv)` entry point, and emits named error code constants with `ERROR {CODE}: {message}` output. Negative fixtures assert against stable codes. The two-positional signature (`artifact_id` + `artifact_path`) is preserved as the documented exception.

**Key interfaces:**
- CLI: `python validate-artifact.py <artifact_id> <artifact_path> [--repo-root PATH] [--list-codes]`
- `main(argv list | None) -> int` entry point
- Uses `load_artifact_contracts` from shared utility module
- Module-level error code constants: ARTIFACT_FILE_NOT_FOUND, CONTRACTS_FILE_NOT_FOUND, CONTRACT_NOT_FOUND, ABSOLUTE_FILE_LINK, MISSING_REQUIRED_SECTION, MISSING_YAML_BLOCK, MISSING_MACHINE_FIELDS, MISSING_EVIDENCE_EXCERPTS, MISSING_EXCERPT_FIELD, ABSOLUTE_EXCERPT_PATH
- Error output format: `ERROR {CODE}: {message}`

**Acceptance criteria:**
- [ ] Uses shared `load_artifact_contracts` from the utility module
- [ ] `--list-codes` prints all stable error codes and exits 0
- [ ] `main(argv)` callable programmatically
- [ ] Error output uses `ERROR {CODE}: {message}` format
- [ ] Negative fixture frontmatter uses stable codes
- [ ] `python scripts/test-validators.py` passes

**Out of scope:**
- No changes to the two-positional signature (kept as documented exception)
- No changes to the evidence_excerpts validation logic
