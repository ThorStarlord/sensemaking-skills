> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Create shared utility module for registry loading and error formatting; refactor validate-brief to use it

**Current behavior:**
Four validators independently load and parse YAML registry files (workflow-registry.yaml, artifact-contracts.yaml, skill-registry.yaml). Each has its own error handling and path resolution. validate-brief.py has an inline `_load_weakness_types` function.

**Desired behavior:**
A single shared module provides pure functions for all common operations. validate-brief.py imports from this module instead of defining its own. All existing tests pass with no behavioral changes.

**Key interfaces:**
- Module named with leading underscore (excluded from test auto-discovery by naming convention)
- `load_yaml(path)` — safe YAML load with error wrapping
- `load_workflow_registry(repo_root)`, `load_artifact_contracts(repo_root)`, `load_skill_registry(repo_root)`, `load_weakness_types(repo_root)`
- `format_error(code, message)` — returns "ERROR {code}: {message}"
- `resolve_repo_root(given, script_dir)` — resolve relative repo root path
- validate-brief.py replaces inline loading with these imports; same validation logic, same stable error codes, same CLI

**Acceptance criteria:**
- [ ] The shared module exists with all listed functions
- [ ] validate-brief.py no longer defines its own `_load_weakness_types` or inline YAML loading
- [ ] Both modules handle `--help` and `--list-codes` without errors
- [ ] `python scripts/test-validators.py` passes (all 34 tests)
- [ ] `python scripts/validate-repo.py` passes

**Out of scope:**
- No behavioral changes to any validator
- No new stable error codes in validators that lack them (handled in later slices)
- No changes to the test harness
