> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Standardize validate-skill-improvement-plan with stable error codes and consistent CLI

**Current behavior:**
validate-skill-improvement-plan.py has no `--list-codes`, no `main(argv)` entry point, uses free-text error messages, and names its positional argument `plan_path`.

**Desired behavior:**
The validator conforms to the standard contract: accepts `artifact_path` (positional), `--repo-root`, and `--list-codes`. Has a callable `main(argv)` entry point. Emits named error code constants and outputs `ERROR {CODE}: {message}`. Negative fixtures assert against stable codes.

**Key interfaces:**
- Standard CLI: `python validate-skill-improvement-plan.py <artifact_path> [--repo-root PATH] [--list-codes]`
- `main(argv list | None) -> int` entry point
- Module-level error code constants: IMPROVEMENT_FILE_NOT_FOUND, MISSING_SECTION, INVALID_FAILURE_MODE_CLASS, INVALID_DEFECT_SOURCE, MISSING_SOURCE_REPORT, SOURCE_REPORT_NOT_FOUND, ABSOLUTE_SOURCE_REPORT_PATH, MISSING_EVIDENCE_SNIPPET, INVALID_RECOMMENDED_ACTION, MISSING_EDIT_TYPE, MISSING_RISK_LEVEL, MISSING_ANTI_OVERFITTING, MISSING_RERUN_SCENARIO, MISSING_SUCCESS_CRITERIA, ABSOLUTE_PATH_DETECTED
- Error output format: `ERROR {CODE}: {message}`

**Acceptance criteria:**
- [ ] `--list-codes` prints all stable error codes and exits 0
- [ ] `main(argv)` entry point callable programmatically
- [ ] Error output uses `ERROR {CODE}: {message}` format
- [ ] Negative fixture frontmatter uses stable codes
- [ ] `python scripts/test-validators.py` passes

**Out of scope:**
- No changes to the 10 failure mode class checks or defect source validation logic
- No changes to other validators
