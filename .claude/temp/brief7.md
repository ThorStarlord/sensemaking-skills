> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Standardize validate-plan with shared utilities, stable error codes, and consistent CLI

**Current behavior:**
validate-plan.py loads all 3 registries inline (duplicated), has no `--list-codes`, no `main(argv)` entry point, uses free-text error messages, and names its positional argument `plan_path`. It already has `--repo-root`.

**Desired behavior:**
The validator uses the shared utility module for registry loading. Has `--list-codes`, a `main(argv)` entry point, and emits named error code constants with `ERROR {CODE}: {message}` output. Negative fixtures assert against stable codes.

**Key interfaces:**
- Standard CLI: `python validate-plan.py <artifact_path> [--repo-root PATH] [--list-codes]`
- `main(argv list | None) -> int` entry point
- Uses shared `load_workflow_registry`, `load_artifact_contracts`, `load_skill_registry` from utility module
- Module-level error code constants: WORKFLOW_NOT_FOUND, EXECUTION_MODE_DENIED, INPUT_MISMATCH, STEP_COUNT_MISMATCH, STEP_SKILL_MISMATCH, STEP_TYPE_MISMATCH, GATE_MISMATCH, INPUT_ARTIFACT_MISMATCH, OUTPUT_ARTIFACT_MISMATCH, ARTIFACT_NOT_CONTRACTED, GATE_BEHAVIOR_MISSING, SIMULATED_GATE_CLASH, STOP_CONDITIONS_EMPTY, SUBSET_NOT_CONTIGUOUS, SECTION_11_MALFORMED, ABSOLUTE_PATH_DETECTED
- Error output format: `ERROR {CODE}: {message}`

**Acceptance criteria:**
- [ ] Uses shared registry loading functions
- [ ] `--list-codes` prints all stable error codes and exits 0
- [ ] `main(argv)` callable programmatically
- [ ] Error output uses `ERROR {CODE}: {message}` format
- [ ] Negative fixture frontmatter uses stable codes
- [ ] `python scripts/test-validators.py` passes

**Out of scope:**
- No changes to the 9-step validation logic (subset_run, step matching, gate validation, etc.)
- No changes to other validators
