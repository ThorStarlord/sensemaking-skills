# Repository Sensemaking Brief

<!-- artifact_id: repository_sensemaking_brief | schema_version: 1 -->
<!-- target_repo: experiments/repository-sensemaking-skill-hardening-v1/corpus/generated-heavy -->

## 1. Repository goal

OBSERVED: the repository's only documentation, `README.md:3`, states `Protobuf-based project.` — a one-line declaration that this is a protobuf-based project. The directory layout (`handwritten/`, `generated/`, `dist/`) implies the classic protobuf workflow: hand-authored code consuming compiler-generated bindings, plus built distribution artifacts. The apparent goal is a minimal protobuf project: a `Message` type (`generated/api_pb2.py:2`) consumed by the handwritten entry point (`handwritten/main.py:1-4`).

INFERRED: beyond "protobuf-based", no user-facing goal, feature list, or acceptance criteria is documented anywhere — there is no roadmap, no product documentation, and no issue tracker content in the repository. UNKNOWN: whether the intended end state is a working message-serialization demo, a gRPC service, or a packaging exercise — nothing in the repository establishes this.

## 2. Current shape

### Inventory (OBSERVED — full recursive listing, 6 files total)

- `README.md` — 3 lines: title plus "Protobuf-based project."
- `handwritten/main.py` — 4 lines: the only hand-authored code.
- `generated/api_pb2.py` — 3 lines; `generated/api_pb2_grpc.py` — 1 line.
- `dist/app.py` — 1 line; `dist/bundle.js` — 1 line.
- **Absent** (Pass A): any manifest (no `pyproject.toml`, `setup.py`, `requirements.txt`, `package.json`, `go.mod`), any `.proto` file, any build configuration, any CI configuration, any container/deployment configuration, any tests, any documentation beyond the README.

### Runtime flow (reconstructed — Pass B/C)

- **Startup**: nothing launches anything; the single executable path is `handwritten/main.py` run directly. Pass B found no other entry-point declarations: no package scripts, no CLI commands, no server bootstrap, no route registration, no workers/jobs, no plugin registration.
- **Orchestration**: `handwritten/main.py:1-4` is straight-line module-level code: import → construct → print. Nothing orchestrates beyond that.
- **Domain/core logic**: none — the only domain type, `Message`, is an empty class (`generated/api_pb2.py:2-3`).
- **Persistence/state**: none — no files, database, cache, queue, environment variables, or remote state anywhere.
- **External integrations**: none — no network calls; `generated/api_pb2_grpc.py:1` contains no stubs or servicers, so even the nominal gRPC surface is inert.
- **Background work**: none.
- **Output boundary**: stdout — `print(m)` at `handwritten/main.py:4`.

### Dependency semantics

- `generated.api_pb2` — `declared`: cannot be established from any manifest (none exists); `used`: imported at `handwritten/main.py:1`; `runtime`: exercised on the only executable path (import at `:1`, construct at `:3`, print at `:4`). But import exists ≠ protobuf semantics exist: the imported surface is an empty class (`generated/api_pb2.py:2-3`).

### State model

No state boundaries exist (no files/db/cache/global state/queues/env/remote systems) — the repository is stateless.

### Boundary model

The only genuine responsibility transition is handwritten → generated, at the import in `handwritten/main.py:1`. The other transitions are nominal: `generated/` is supposed to be compiler output and `dist/` is supposed to be build output, but no tooling exists that could produce either, so those responsibility boundaries are unenforced (see Section 6).

### Where responsibility becomes unclear

The `generated/` and `dist/` directories claim provenance (protobuf compiler; build/bundler) that no file in the repository can reproduce. The "generated" and "built" labels are the least trustworthy claims in the repository.

## 3. Strong signals

- Conventional layout: `handwritten/generated/dist` is a recognizable, conventional project shape (OBSERVED in the directory inventory).
- Provenance discipline is *attempted*: both generated files carry "DO NOT EDIT" headers (`generated/api_pb2.py:1`; `generated/api_pb2_grpc.py:1`) — the author understood that generated content should be marked and not hand-edited.
- The handwritten entry point imports through the package path (`handwritten/main.py:1`) rather than a flat-file hack, so a real generated package could slot in without changing the caller.
- Deterministic, side-effect-free execution: `handwritten/main.py:1-4` terminates in four statements with no hidden I/O.

## 4. Missing pieces

- **`.proto` source of truth — absent**: full recursive inventory contains zero `.proto` files, so `generated/` can never be regenerated by any tool.
- **Manifest/package metadata — absent**: no `pyproject.toml`, `setup.py`, `requirements.txt`, or `package.json`.
- **Build pipeline for `dist/` — absent**: no bundler config, no build script, no CI step that could produce `dist/app.py:1` or `dist/bundle.js:1`.
- **Message semantics — absent**: `Message` has no fields and no serialization (`generated/api_pb2.py:2-3`).
- **gRPC API surface — absent**: `generated/api_pb2_grpc.py:1` is only a header comment.
- **Tests, CI, validation — absent** (Pass D): no test files, no schema files, no assertions, no authorization or error-boundary code.
- **Documentation beyond the one-line README — absent**.

## 5. Improvement opportunities

- Introduce the missing `.proto` file and a reproducible codegen step so `generated/` becomes genuinely regenerable and its "DO NOT EDIT" headers become truthful.
- Add a minimal manifest and a real build script so `dist/` can actually be produced (or remove `dist/` until one exists).
- Add a smoke test asserting the output of `handwritten/main.py:1-4`.
- Expand `README.md:3` into a description of the intended flow and the regeneration command.
- If the repository is intentionally a stub or fixture, say so in the README so the "Protobuf-based" claim stops being a contract.

## 6. Weakest boundary

### Candidate generation and scoring (2-5 candidates, per SKILL.md)

1. **Generated-surface contract** (`generated/api_pb2.py:1-3` + `generated/api_pb2_grpc.py:1` claiming compiler provenance, with no `.proto` anywhere): evidence_strength `strong`; severity `high`; blast_radius `high`; goal_relevance `high`; downstream_blocking_effect `high`; uncertainty `low`.
2. **README promise vs implementation** (`README.md:3` "Protobuf-based project."): evidence_strength `strong`; severity `medium`; blast_radius `medium`; goal_relevance `medium`; downstream_blocking_effect `medium`; uncertainty `low`.
3. **`dist/` build artifacts with no build pipeline** (`dist/app.py:1`, `dist/bundle.js:1`): evidence_strength `strong`; severity `low`; blast_radius `low`; goal_relevance `low`; downstream_blocking_effect `low`; uncertainty `low`.
4. **Zero validation across the repo** (no tests, no CI, no schemas): evidence_strength `strong`; severity `medium`; blast_radius `high`; goal_relevance `medium`; downstream_blocking_effect `medium`; uncertainty `low`.

Selection: candidate 1, per the selection rule (highest consequence, strongest evidence, most central to the repo's stated goal, and the one that blocks all valuable downstream work). Candidates 3 and 4 are real but are consequences rather than causes — even a perfect test suite would leave the generated surface fake.

Boundary:
The handwritten↔generated contract — `handwritten/main.py:1` imports from `generated/api_pb2.py`, a module that declares itself protobuf compiler output but is an empty hand-authored stub, with no `.proto` source anywhere in the repository to regenerate it from.

Observed contract:
`README.md:3` declares the repository "Protobuf-based project."; `generated/api_pb2.py:1` declares "Generated by the protocol buffer compiler. DO NOT EDIT!"; `generated/api_pb2_grpc.py:1` declares "Generated. DO NOT EDIT!"; `handwritten/main.py:1-4` imports `Message` from that surface and constructs it. The implied contract: a regenerable, compiler-produced protobuf binding layer that handwritten code consumes.

Observed violation or uncertainty:
The "generated" files are not compiler output and cannot become it. The entire content of `generated/api_pb2.py:2-3` is `class Message:` plus `pass` — no fields, no descriptors, no serialization — and `generated/api_pb2_grpc.py:1` has no API surface at all (a gRPC-named file with only a header). No `.proto` file exists anywhere in the repository (full recursive inventory: 6 files, zero `.proto`), so no tool invocation could reproduce this directory. The import at `handwritten/main.py:1` succeeds only because the stub happens to exist; it provides none of the protobuf semantics the README promises.

Evidence:
`generated/api_pb2.py:1-3`; `generated/api_pb2_grpc.py:1`; `handwritten/main.py:1-4`; `README.md:3`; `dist/app.py:1`; `dist/bundle.js:1`; absence of `.proto`/manifest/build config (observed in the full recursive listing).

Weakness type:
**Weakness type:** Contract Mismatch

Logic trace:
`README.md:3` promises a protobuf-based project, and `generated/api_pb2.py:1` plus `generated/api_pb2_grpc.py:1` declare themselves protobuf compiler output, which `handwritten/main.py:1` consumes by import. But the observed body of those "generated" files is an empty class (`generated/api_pb2.py:2-3`) and a bare header (`generated/api_pb2_grpc.py:1`), and the full recursive inventory contains no `.proto` source from which they could ever be regenerated and no build configuration that could produce `dist/app.py:1` or `dist/bundle.js:1`. The files therefore assert a nature (compiler-generated, regenerable, protobuf-semantic) that they do not have — a mismatch between what each file claims to be and what it actually is, at the repository's only real responsibility boundary. That is the definition of Contract Mismatch, and because the repository's entire stated purpose ("protobuf-based") rests on this surface, it is the weakest boundary.

Failure consequence:
Every downstream action passes through the generated surface. Adding message fields requires editing a file whose header forbids editing; regenerating bindings is impossible because no `.proto` exists; building a gRPC service is impossible because `generated/api_pb2_grpc.py:1` has no surface; trusting the README leads a contributor to believe protobuf behavior exists when it does not. Any implementation attempt either (a) silently hand-edits "generated" code, cementing the fiction, or (b) discovers the truth mid-work and must re-scope the entire repository. Wrong assumptions at this boundary propagate to everything else in the repo.

Confidence:
High — 100% of the repository was inspected (6 files, every file read in full); the stub content is directly observed, and the absence of `.proto`/manifest/build config is observed in the complete recursive listing, so the derivation is short and unambiguous. What would raise it further: an authoritative statement of intent (is the repository meant to be a stub fixture or a real protobuf project?) — but the classification holds either way.

Alternatives considered:
- **Ghost Features** (`README.md:3` "Protobuf-based project." with no implementing surface) — lost because the loudest false claim sits at the file level (the "DO NOT EDIT" compiler-provenance headers on files that were hand-authored), which is a contract property of the files themselves, not merely a documentation promise; the README line largely restates what the file headers assert.
- **Zero Validation** (no tests/CI anywhere) — lost because it is downstream of the contract defect: a test suite would expose the fabricated surface but not fix it; the contract mismatch is the cause.
- **Ghost Features / Orphaned Examples** (`dist/app.py:1`, `dist/bundle.js:1` placeholder build outputs with no build pipeline) — lost on severity and goal relevance: nothing imports or consumes `dist/`, and build artifacts are peripheral to the repo's protobuf claim.
- **Vocabulary Drift** ("protobuf" term in README vs code) — lost: the term does match the directory structure (`generated/api_pb2.py` exists and is imported), so the drift is not in vocabulary but in the substance of the files.

## 6.5. Problem classification (fog type)

primary_fog_type: **architecture_fog**.

Evidence chain: the repository's responsibility boundaries are fictional. "handwritten" vs "generated" vs "dist" imply a pipeline (author → protoc → build) whose tooling does not exist in the repository (absence observed in the full inventory). The handwritten entry point (`handwritten/main.py:1`) depends on a generated surface that cannot be regenerated and carries no protobuf semantics (`generated/api_pb2.py:2-3`, `generated/api_pb2_grpc.py:1`). This is a structural mismatch between the declared flow and the actual flow — the definition of architecture_fog (module boundaries unclear, unwired modules, structural mismatch between entry points and flow).

Why not the others:
- **ui_fog**: no — there is no frontend code (no React/Vue/Angular/HTML/CSS; `dist/bundle.js:1` is a single `console.log` inside a build-output directory). Per the UI Fog Signals Registry decision tree, missing frontend → not ui_fog.
- **docs_fog**: not primary — the README is thin but does not misdescribe the structure; the defect lives in file contracts and structure, not in documentation coverage or staleness.
- **product_fog**: no — `README.md:3` makes no feature promise (no roadmap, no feature list, no deliverable); it is a type declaration. Applying the ghost-feature reasoning, the mismatch lives in the structure (the architecture cannot support real protobuf content), not in a product contract.

## 7. Evidence

All evidence below is OBSERVED from files opened in full (each file is at most 4 lines, so every citation covers the file's complete content). The contradiction is stark and fully contained:

- `generated/api_pb2.py:1` claims protobuf compiler provenance while `generated/api_pb2.py:2-3` contain only an empty class.
- `generated/api_pb2_grpc.py:1` is a "generated" file with no API surface at all.
- `handwritten/main.py:1` imports from that surface; `handwritten/main.py:3-4` constructs and prints the empty stub.
- `README.md:3` calls the project "Protobuf-based".
- `dist/app.py:1` and `dist/bundle.js:1` are placeholder build outputs ("built artifact", "minified").
- The full recursive inventory of the repository (6 files) contains no `.proto` file, no manifest, no build configuration, and no tests.

Logic trace: `README.md:3` promises a protobuf-based project, and the directory layout (`handwritten/`, `generated/`, `dist/`) promises the standard protobuf pipeline: hand-authored code consuming compiler-generated bindings, plus built artifacts. `generated/api_pb2.py:1` and `generated/api_pb2_grpc.py:1` declare themselves compiler output, and `handwritten/main.py:1` depends on that surface by import. But `generated/api_pb2.py:2-3` contains only an empty class — no fields, no serialization — and `generated/api_pb2_grpc.py:1` contains nothing at all; the full recursive inventory shows no `.proto` file from which these files could ever be regenerated, and no build configuration that could produce `dist/app.py:1` or `dist/bundle.js:1`. The chain therefore lands on the boundary where responsibility changes hands — handwritten code → "generated" bindings — as the weakest point: the file that owns the protobuf contract does not deliver it, cannot be regenerated, and its provenance claim is false. That is a Contract Mismatch at the repository's only real boundary, and because the repository's entire purpose ("protobuf-based") rests on this surface, the structural reading (architecture_fog) is the correct primary classification.

## 8. Evidence excerpts

```yaml
evidence_excerpts:
  - file: generated/api_pb2.py
    lines: L1-L3
    quote: "# Generated by the protocol buffer compiler. DO NOT EDIT!\nclass Message:\n    pass"
    supports_claim: "The module declares compiler provenance but its entire body is an empty class - the generated surface is a hand-authored stub."
  - file: generated/api_pb2_grpc.py
    lines: L1
    quote: "# Generated. DO NOT EDIT!"
    supports_claim: "The gRPC 'generated' file has no stubs, servicers, or any API surface at all."
  - file: handwritten/main.py
    lines: L1
    quote: "from generated.api_pb2 import Message"
    supports_claim: "The only handwritten entry point depends on the fabricated generated surface by import."
  - file: handwritten/main.py
    lines: L3-L4
    quote: "m = Message()\nprint(m)"
    supports_claim: "The entry point constructs the empty stub with no fields and prints it; no protobuf semantics are exercised."
  - file: README.md
    lines: L3
    quote: "Protobuf-based project."
    supports_claim: "The README's only substantive claim is a protobuf promise the code does not honor."
  - file: dist/app.py
    lines: L1
    quote: "print('built artifact')"
    supports_claim: "The 'built' artifact is a placeholder string and no build pipeline exists that could produce it."
```

## 9. Why this boundary matters

Every downstream action on this repository passes through the generated surface. Adding message fields requires editing a file whose header forbids editing; regenerating bindings is impossible because no `.proto` exists; building a gRPC service is impossible because `generated/api_pb2_grpc.py:1` has no surface; trusting the README leads a contributor to believe protobuf behavior exists when it does not. The boundary is the single point where a wrong assumption propagates to everything else: if this contract stays weak, any implementation work either hand-edits "generated" code (cementing the fiction) or is re-scoped mid-flight when the truth surfaces. Until the boundary is restored, no feature, service, or packaging work can be planned with confidence.

## 10. Candidate next steps

1. Establish the source of truth: add a `.proto` file (message, and optionally a service) and a reproducible protoc/codegen step; regenerate `generated/` and verify `handwritten/main.py:1-4` still runs.
2. Decide and document intent: state in `README.md` whether the repository is a real protobuf project or a deliberate stub; this single decision unblocks everything else.
3. Add a manifest and a real build step for `dist/`, or remove `dist/` until a build pipeline exists.
4. Add a smoke test asserting the output of `handwritten/main.py:4` so the handwritten→generated contract is at least mechanically checked.
5. Run a spec-driven architecture review of the handwritten/generated/dist boundary before any feature work.

## 11. Recommended next step

The smallest, highest-leverage action: restore the truthfulness of the generated-surface contract — add the missing `.proto` source of truth and a reproducible regeneration step, regenerate `generated/api_pb2.py` and `generated/api_pb2_grpc.py` so their "DO NOT EDIT" headers become accurate, and confirm `handwritten/main.py:1-4` still runs unchanged. This is the precondition for every other improvement and it directly dissolves the Contract Mismatch.

## 12. Recommended workflow

`architecture-implementation-workflow` from `skills/workflow-planner/references/workflow-registry.yaml` (registry lines 848-904), with execution mode `guided_execution` — one of that workflow's `allowed_execution_modes` (registry lines 858-860). This is the fog-matched implementation path for architecture_fog: it aligns domain understanding, produces an architecture/refactoring spec that can define the `.proto` source-of-truth and regeneration step, decomposes into issues, and implements via TDD.

Why not the closest alternatives: `ui-diagnostic-workflow` / `ui-implementation-workflow` (registry lines 715-811) require ui_fog — the repository has no frontend (`dist/bundle.js:1` is a build-output placeholder); `docs-implementation-workflow` (registry lines 812-847) is for docs_fog — the defect is not documentation coverage; `product-implementation-workflow` (registry lines 644-714) is for product_fog — no feature promise exists; the generic `implementation-workflow` (registry lines 587-643) would function but the fog-specialized architecture workflow matches the diagnosis more precisely. `plan_only` is deliberately NOT offered by `architecture-implementation-workflow` (registry lines 858-860 list only `guided_execution` and `autonomous_execution`), so `guided_execution` is the compatible, human-gated choice for a diagnostic handoff; recommending it here does not execute anything — it only routes the next step.

## 13. Machine-readable handoff

```yaml
artifact_id: repository_sensemaking_brief
schema_version: 1
target_repo: H:/GithubRepositories/sensemaking-skills/experiments/repository-sensemaking-skill-hardening-v1/corpus/generated-heavy
source_intent_ref: artifacts/01-orchestration-run/00-user-intent.md
workflow_registry_source: skills/workflow-planner/references/workflow-registry.yaml
user_implied_fog_type: unknown
primary_fog_type: architecture_fog
diagnosis_conflict: false
escalation_recommended: false
escalation_required: false
evidence:
  - "README.md (line 3): 'Protobuf-based project.' - protobuf promise with no implementing surface"
  - "generated/api_pb2.py (lines 1-3): header claims compiler provenance; body is an empty class stub"
  - "generated/api_pb2_grpc.py (line 1): 'generated' gRPC file with no API surface"
  - "handwritten/main.py (lines 1, 3-4): only entry point imports and constructs the stub Message"
  - "dist/app.py (line 1): placeholder build artifact with no build pipeline"
  - "dist/bundle.js (line 1): placeholder minified bundle with no build pipeline"
recommended_workflow_id: architecture-implementation-workflow
recommended_execution_mode: guided_execution
weakest_boundary: Contract Mismatch
weakness_type: Contract Mismatch
weakness_type_explanation: null
required_inputs:
  - user_intent
  - repository_state
created_at: "2026-06-18T12:00:00Z"
immutable: true
```

## 14. Ready-to-copy prompt

Handoff to `workflow-planner` (to route into `architecture-implementation-workflow`):

"Run architecture-implementation-workflow in guided_execution mode against the repository at experiments/repository-sensemaking-skill-hardening-v1/corpus/generated-heavy. The repository sensemaking brief classifies it as architecture_fog with weakest boundary Contract Mismatch: generated/api_pb2.py:1-3 and generated/api_pb2_grpc.py:1 declare protobuf compiler provenance but are empty hand-authored stubs, no .proto file exists anywhere in the repository, handwritten/main.py:1 imports from that surface, and dist/ contains placeholder build outputs (dist/app.py:1, dist/bundle.js:1) with no build pipeline. Start with the architecture/refactoring spec: define the .proto source of truth and a reproducible codegen step so the 'DO NOT EDIT' headers become truthful, then decompose into issues and implement via TDD with a smoke test on handwritten/main.py:1-4. Do not hand-edit the 'generated' files as a workaround."
