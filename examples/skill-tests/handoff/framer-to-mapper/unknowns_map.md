# Unknowns Map (Handoff Test)

## 1. Knowns
- (Derived from Wave 1 Problem Frame)
- Goal: Transform messy ideas into workflows.
- OUP: `workflow-registry.yaml`.
- Failure: Hallucinated workflows.

## 2. Unknowns
- Schema details of `workflow-registry.yaml`.
- User-specific scenarios to prioritize.

## 3. Assumptions
- A three-stage sensemaking pipeline is sufficient for bridge-building.

## 4. Risks
- Skill composition failure (the skills don't actually fit the workflows).

## 5. Research Paths
- Pattern audit of `workflow-registry.yaml`.

## 6. Stopping Rule
- Stop when the search seed for the registry is defined and `repo-sensemaker` handoff requirements are met.

---
## Handoff Quality Audit
- **Producer Artifact**: `problem_frame.md` (Task 8.1)
- **Consumer Skill**: `unknowns-mapper`
- **Result**: **HIGH QUALITY**.
- **Evidence**: The `problem_frame.md` provided a clear `Object Under Pressure` (`workflow-registry.yaml`) and a specific `Failure Mode` (hallucinated workflows), which allowed the Mapper to immediately define relevant `Unknowns` and `Research Paths` without asking for clarification.
